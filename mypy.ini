[mypy]
plugins = pydantic.mypy

# gliner (Phase 4, Tier 2) ships no type stubs or py.typed marker.
# Scoped to just this package, not a blanket ignore_missing_imports -
# every other third-party import in this project still gets checked.
[mypy-gliner.*]
ignore_missing_imports = True
