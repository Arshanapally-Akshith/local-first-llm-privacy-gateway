"""The fixed, deterministic workload matrix `latency/runner/` drives at
every concurrency level. See `definitions.py` for the eight workloads
and `workload_types.py` for the shared `LatencyWorkload` type.
"""
