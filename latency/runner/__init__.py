"""The Phase 7 latency harness's own runner: process lifecycle
(`process_harness.py`), log correlation (`log_capture.py`), request
execution (`measure.py`), statistics (`stats.py`), and the
`build_report()`/`main()` entrypoint (`run.py`) — mirroring the shape
`benchmarks/runner/run.py`, `adversarial/runner/run.py`, and
`rehydration_fidelity/runner/run.py` already share.
"""
