[pytest]
markers =
    real_model: exercises a real, downloaded ML model (network on first run; heavy CPU/RAM always) - excluded from the default run. Run explicitly with `pytest -m real_model`.
addopts = -m "not real_model"
