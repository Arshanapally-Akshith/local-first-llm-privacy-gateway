"""One module per bypass class, each exposing `build_cases()`.

Deliberately empty beyond this docstring: `adversarial/cases/discovery.py`
finds every bypass-class module by walking this package's contents at
runtime, not by importing them here — adding a new class means adding
a new module, nothing else.
"""
